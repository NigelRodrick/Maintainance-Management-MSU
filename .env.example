# Copy to .env and adjust (never commit .env)
FLASK_ENV=development
SECRET_KEY=replace-with-a-long-random-string
USE_SQLITE=1
# MSU_DEMO_PASSWORD=YourSecurePassword123!

# SQL Server (optional): run msu_maintenance_system/scripts/setup_sql_server.ps1 — it writes
# %LOCALAPPDATA%\MSUMaintenance\.env with USE_SQLITE=0 and Docker SQL credentials.
# USE_SQLITE=0
# DB_SERVER=localhost
# DB_NAME=CentralServices_AM_DB
# DB_USER=sa
# DB_PASSWORD=MsuSqlSetup!2026Strong
